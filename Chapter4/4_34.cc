
int main() {
	float fval;
	double dval;
	int ival;

	/*
	In conditions non zero double
	values are converted to true, zero to false

	if (fval) 
	*/

	/*int value converted to float in fval + ival
	float converts to double in assignment

	dval = fval + ival; */

	/*Integral promotion of cval to int
	/* result of product is then converted to double
	

	dval + ival * cval */


	return 0;
}