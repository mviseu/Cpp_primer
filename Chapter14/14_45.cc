#include "Sales_data_14_45.h"
#include <iostream>
#include <string>

int main() {
	//double db = static_cast<double>(Sales_data("Hello"));
	std::string str = static_cast<std::string>(Sales_data("Hello"));
	//std::cout << db << std::endl;
	std::cout << str << std::endl;
	return 0;
}