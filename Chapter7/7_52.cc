#include "7_52.h"

int main() {
	//we can initialize aggregate classes with a braced list of initializers
	//non of the members must hhave in-class initializers
	Sales_data item = {"9-99", 25, 15.99};

	return 0;
}